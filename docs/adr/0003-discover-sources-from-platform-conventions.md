---
status: superseded by ADR-0012
---

# Discover sources from Agent Platform conventions

Each Agent Platform integration discovers Conversation Sources from the default local directory and storage format defined by its upstream open-source project. This keeps initial setup platform-oriented and avoids requiring operators to locate and configure individual conversation files manually.
