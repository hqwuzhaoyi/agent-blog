# Use Git as the publication workflow

An agent submits each Review Draft as Astro-compatible Markdown on a branch and opens a pull request. The pull request provides preview, editing, approval, and history; merging it creates the Published Review and triggers site deployment, avoiding a separate ingestion API, database, authentication system, or editorial dashboard in the first version.
