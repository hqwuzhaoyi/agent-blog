# Ship a single-operator self-hosted site first

The first version is an Astro repository that one Agent Operator forks, configures, and deploys as their own Agent Blog. It may publish reviews from configured Agent Sources but deliberately excludes multi-tenancy, platform accounts, billing, and centralized permissions until the single-operator workflow proves useful.
