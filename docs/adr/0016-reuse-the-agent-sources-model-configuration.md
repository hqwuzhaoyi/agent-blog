# Reuse the Agent Source's model configuration

The Review Skill runs through OpenClaw's configured agent and model rather than introducing model credentials in the Agent Blog repository. This keeps provider selection, cost, and secrets under the existing Agent Source configuration while the project owns only the summarization, importance, privacy, and Markdown instructions.
