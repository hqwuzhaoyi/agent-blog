# Domain Docs

This is a single-context repository.

Before working in the repository, read:

- The root `CONTEXT.md` glossary
- Relevant decisions under `docs/adr/`

Use the glossary's canonical terms in specs, issues, tests, interfaces, and implementation. If proposed work contradicts an accepted ADR, surface the conflict explicitly. ADRs marked as superseded describe history and are not current decisions.
