---
import { siteConfig } from "@/site.config";
import { sitePath } from "@/lib/paths";
import "@/styles/global.css";

interface Props {
  title?: string;
  description?: string;
  pageClass?: string;
}

const {
  title = siteConfig.title,
  description = siteConfig.description,
  pageClass = "",
} = Astro.props;
const pageTitle = title === siteConfig.title ? title : `${title} — ${siteConfig.title}`;
const canonical = new URL(Astro.url.pathname, Astro.site);
const themeColor = siteConfig.theme === "signal-console" ? "#94ff65" : "#f05a28";
---

<!doctype html>
<html lang={siteConfig.language} data-theme={siteConfig.theme}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta name="theme-color" content={themeColor} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href={sitePath("favicon.svg")} />
    <link rel="alternate" type="application/rss+xml" title={`${siteConfig.title} RSS`} href={sitePath("rss.xml")} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <title>{pageTitle}</title>
  </head>
  <body class={pageClass}>
    <div class="page-shell">
      <header class="site-header">
        <a class="wordmark" href={sitePath()} aria-label={siteConfig.title}>
          <span class="wordmark__signal" aria-hidden="true"></span>
          <span>
            <strong>{siteConfig.title}</strong>
            <small>{siteConfig.eyebrow}</small>
          </span>
        </a>
        <nav aria-label={siteConfig.nav.label}>
          <a href={sitePath()}>{siteConfig.nav.latest}</a>
          <a href={sitePath("archive/")}>{siteConfig.nav.archive}</a>
          <a href={sitePath("rss.xml")}>{siteConfig.nav.rss}</a>
        </nav>
      </header>
      <main>
        <slot />
      </main>
      <footer class="site-footer">
        <div>
          <span class="status-light" aria-hidden="true"></span>
          <span>{siteConfig.footer.reporting} {siteConfig.sourceLabel}</span>
        </div>
        <p>{siteConfig.footer.disclaimer}</p>
      </footer>
    </div>
  </body>
</html>
